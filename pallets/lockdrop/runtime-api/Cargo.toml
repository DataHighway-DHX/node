[package]
name = "lockdrop-runtime-api"
version = "0.1.0"
authors = ['Ilya Beregovskiy']
edition = "2021"

[dependencies]
sp-api = { git = 'https://github.com/paritytech/substrate', rev = '50ab759f7a3194173ce8da0937ec2d888dee10e0', default-features = false}

[features]
default = ["std"]
std = [
	"sp-api/std",
]
