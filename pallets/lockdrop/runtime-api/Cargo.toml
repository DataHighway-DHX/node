[package]
name = "lockdrop-runtime-api"
version = "0.1.0"
authors = ['Ilya Beregovskiy']
edition = "2018"

[dependencies]
sp-api = { git = 'https://github.com/paritytech/substrate', rev = 'ac1f7ff32bf0d0e9c328cfe083bee7492c639946', default-features = false}

[features]
default = ["std"]
std = [
	"sp-api/std",
]
